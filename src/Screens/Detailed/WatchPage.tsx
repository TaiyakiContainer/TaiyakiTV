import React from 'react';
import { StyleSheet, View } from 'react-native';

export const DetailWatchPage = () => {
return (
    <View style={styles.view}>
        
    </View>
)
};

const styles = StyleSheet.create({
 view: {
     flex: 1,
 }
});
